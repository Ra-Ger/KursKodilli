package com.kodilla.good.patterns.challenges.posComponents;

public class ProductOrderServiceTemplate implements IOrderService{
    @Override
    public boolean order(User user, IProduct product) {
        return true;
    }
}
